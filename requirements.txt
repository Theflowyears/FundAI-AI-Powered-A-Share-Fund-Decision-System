# fundai 运行依赖
#
# 主流程**只用标准库**（http.client / sqlite3 / http.server / json），
# 因此零依赖也能跑：CLI、每日研判、回测、Web 界面、十年消息库统计。
#
# 下面这个是**可选加速项**：装上后事件模型与情绪回补走向量化，快约 10 倍；
# 不装会自动回退到纯 Python 实现（结果一致，只是慢一些）。
numpy>=1.24
